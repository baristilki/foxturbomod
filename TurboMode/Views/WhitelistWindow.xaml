<Window x:Class="TurboMode.Views.WhitelistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Beyaz Liste Yönetimi" Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🛡 Beyaz Liste — Dokunulmayacak Uygulamalar"
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimary}" />
        <TextBlock Grid.Row="1" Margin="0,4,0,12"
                   Text="Şu an çalışan süreçlerden işaretlediklerin Turbo aktifken askıya alınmayacak. Discord, OBS, antivirüs zaten varsayılan olarak korunur."
                   Foreground="{DynamicResource TextSecondary}" FontSize="11" TextWrapping="Wrap" />

        <Border Grid.Row="2" Background="{DynamicResource PanelBrushDim}"
                BorderBrush="{DynamicResource BorderBrushDim}" BorderThickness="1"
                CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ProcessList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,8" BorderBrush="#22222A" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding ProcessName}"
                                                   Foreground="{DynamicResource TextPrimary}" />
                                        <TextBlock Text="{Binding RamLabel}" FontSize="10"
                                                   Foreground="{DynamicResource TextSecondary}" />
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" IsChecked="{Binding IsWhitelisted}"
                                              Content="Dokunma" Foreground="{DynamicResource TextPrimary}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="İptal" Width="100" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="Kaydet" Width="100" Background="{DynamicResource AccentBrush}"
                    Foreground="White" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
