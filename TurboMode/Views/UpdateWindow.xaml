<Window x:Class="TurboMode.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Güncelleme" Height="280" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}" Icon="/Resources/FoxMod.ico">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🔄" FontSize="24" Margin="0,0,12,0" />
            <TextBlock x:Name="TitleLabel" Text="Yeni sürüm hazır"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="VersionLabel"
                   Text="v1.2 → v1.3"
                   Foreground="{DynamicResource TextSecondary}" FontSize="13"
                   Margin="0,0,0,16" />

        <TextBlock Grid.Row="2" x:Name="StatusLabel"
                   Text="İndirilmeye hazır."
                   Foreground="{DynamicResource TextPrimary}" FontSize="12"
                   Margin="0,0,0,10" />

        <ProgressBar Grid.Row="3" x:Name="ProgressBar" Height="6" Maximum="100"
                     VerticalAlignment="Top"
                     Foreground="{DynamicResource AccentBrush}" Background="#2A2A33" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="İptal" Width="100"
                    Margin="0,0,8,0" Click="Cancel_Click" />
            <Button x:Name="ActionButton" Content="Şimdi Güncelle" Width="140"
                    Background="{DynamicResource AccentBrush}" Foreground="White"
                    Click="Action_Click" />
        </StackPanel>
    </Grid>
</Window>
